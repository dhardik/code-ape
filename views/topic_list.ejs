<!DOCTYPE html>
<html lang="en">

<head>
    <title>code-ape</title>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.0/css/bootstrap.min.css">
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.5.1/jquery.min.js"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/popper.js/1.16.0/umd/popper.min.js"></script>
    <script src="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.0/js/bootstrap.min.js"></script>
    <style>
        * {
            box-sizing: border-box;
        }

        #pmain {
            position: fixed;
            left: 50px;
            right: 0;
            top: 60px;
            bottom: 0;
            margin: auto;
            overflow: auto;
        }

        #main {
            position: relative;
            min-height: 1px;
            width: 85%;
            flex-direction: column;
            align-items: stretch;
            box-shadow: 0 3px 10px 0 rgba(115, 143, 147, .3);
            background: rgb(249, 249, 249);
            border: 1px solid #eeeeee;
            border-radius: 2.5px;
            padding: 1.25rem;
            overflow: auto;
            margin: 2% auto 4%;
            font-family: "Helvetica Neue", Helvetica, Arial, sans-serif;
            font-size: 14px;
        }

        .grid-container {
            display: grid;
            grid-column-gap: 10px;
            grid-template-columns: auto auto auto;
            background-color: white;
            padding: 10px;
            margin: 0 auto;
            width: 80%;
            padding-top: 50px;
            font-size: small;
        }
    </style>
</head>

<body>
    <% if(info.role == "admin") { %>
    <%- include ('./admin/navbar') %>
    <% } %>
    <div id="pmain" class="container">
        <u><h2 align="center">Topics</h2></u>
        <div id="main" class="grid-container">
            <% topics.forEach((elem) => { %>
            <div class="card" style="min-width:80%; margin: 5%;">
                <img class="card-img-top" src="<%= elem.pic %>" alt="Card image" style="width:100%">
                <div class="card-body">
                    <h4 class="card-title"><%= elem.title %></h4>
                    <p class="card-text"><%= elem.description %></p>
                    <a href="/practice/<%= elem._id %>" class="btn btn-primary"><i class="fa fa-arrow-right"
                            aria-hidden="true"></i>Solve</a>
                    <% if(info.role == "admin") { %>
                    <a href="/admin/delete_topic?topicId=<%= elem._id %>" class="btn btn-danger"
                        onclick="return confirm('Are you sure you want to delete topic `<%= elem.title %>` ?')"><i
                            class="fa fa-trash-o" aria-hidden="true"></i>Delete</a>
                    <% } %>
                </div>
            </div>
            <% }); %>
        </div>
    </div>
</body>

</html>